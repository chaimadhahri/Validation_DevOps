pipeline {
    agent any
    stages {
        stage('GIT') {
            steps {
		echo "Getting Project from Git"
                git url: 'https://github.com/chaimadhahri/Validation_DevOps.git', branch: 'oumaima'
            }
        }
          stage('Maven clean') {
            steps {
                sh 'mvn clean'
            }
        }
	 stage('Maven compile') {
            steps {
                sh 'mvn compile'
            }
        }
     stage('Maven sonar:sonar') {
            steps {
                sh 'mvn sonar:sonar  -Dsonar.login=admin -Dsonar.password=mimo'
            }
        }
      stage('Tests') {
            steps {
		sh 'mvn test'
              
            }
        }
     stage('NEXUS'){
            steps{
                sh "mvn deploy -DskipTests=true"
            }
        }
       
      stage('Build Docker Image') {
                steps {
                    echo 'Building the Docker image...'
                 //   sh 'docker-compose -f /Bureau/Station ski/Validation_DevOps/docker-compose.yml build'
                   // sh 'docker build -t SkiG3:tag -f /Bureau/Station ski/Validation_DevOps/Dockerfile'
                    sh 'docker build -t yosrbelaam/ski:1.0.0 .'


                }
            }

            stage('Upload to Docker Hub') {
                steps {
                    echo 'Logging in to Docker Hub with a token...'
                    sh 'echo dckr_pat_kJL36ZvN-uk0AyPhJxOeQ1K53UU | docker login -u yosrbelaam --password-stdin'

                    echo 'Pushing the Docker image to Docker Hub...'
                    sh 'docker push yosrbelaam/ski:1.0.0'
                }
            }



            stage('Run Docker Compose') {
                steps {
                    echo 'Running Docker Compose...'
                   // sh 'sudo docker-compose -f /home/vagrant/docker-compose.yml up -d'
                    sh 'docker-compose up -d'
                }
            }



            }


    }

